Run the following command to start the backend server.(Currently still in development)
```bash
cd backend
uvicorn main:app --reload
```

---

Then run the following command to start the front-end server.
```bash
cd frontend
npm run start
```
